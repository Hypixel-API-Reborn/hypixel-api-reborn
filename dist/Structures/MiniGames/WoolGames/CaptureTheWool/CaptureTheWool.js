import Divide from '../../../../Utils/Divide.js';
import InventoryLayout from '../../Shared/InventoryLayout.js';
class CaptureTheWool {
    kills;
    assists;
    mostKillsAndAssists;
    deaths;
    killDeathRatio;
    killsWithWool;
    deathsWithWool;
    killDeathRatioWithWool;
    killsOnWoolHolder;
    deathsToWoolHolder;
    woolCaptured;
    woolStolen;
    woolCaptureStolenRatio;
    mostGoldEarned;
    goldEarned;
    goldSpent;
    experiencedWins;
    experiencedDraws;
    experiencedLosses;
    experiencedWinLossRatio;
    participatedWins;
    participatedDraws;
    participatedLosses;
    participatedWinLossRatio;
    fastestWoolCapture;
    fastestWin;
    longestGame;
    layout;
    constructor(data) {
        this.kills = data?.stats?.kills || 0;
        this.assists = data?.stats?.assists || 0;
        this.mostKillsAndAssists = data?.stats?.most_kills_and_assists || 0;
        this.deaths = data?.stats?.deaths || 0;
        this.killDeathRatio = Divide(this.kills, this.deaths);
        this.killsWithWool = data?.stats?.kills_with_wool || 0;
        this.deathsWithWool = data?.stats?.deaths_with_wool || 0;
        this.killDeathRatioWithWool = Divide(this.killsWithWool, this.deathsWithWool);
        this.killsOnWoolHolder = data?.stats?.kills_on_woolholder || 0;
        this.deathsToWoolHolder = data?.stats?.deaths_to_woolholder || 0;
        this.woolCaptured = data?.stats?.wools_captured || 0;
        this.woolStolen = data?.stats?.wools_stolen || 0;
        this.woolCaptureStolenRatio = Divide(this.woolCaptured, this.woolStolen);
        this.mostGoldEarned = data?.stats?.most_gold_earned || 0;
        this.goldEarned = data?.gold_earned || 0;
        this.goldSpent = data?.gold_spent || 0;
        this.experiencedWins = data?.stats?.experienced_wins || 0;
        this.experiencedDraws = data?.stats?.experienced_draws || 0;
        this.experiencedLosses = data?.stats?.experienced_losses || 0;
        this.experiencedWinLossRatio = Divide(this.experiencedWins, this.experiencedLosses);
        this.participatedWins = data?.stats?.participated_wins || 0;
        this.participatedDraws = data?.stats?.participated_draws || 0;
        this.participatedLosses = data?.stats?.participated_losses || 0;
        this.participatedWinLossRatio = Divide(this.participatedWins, this.participatedLosses);
        this.fastestWoolCapture = data?.stats?.fastest_wool_capture || 0;
        this.fastestWin = data?.stats?.fastest_win || 0;
        this.longestGame = data?.stats?.longest_game || 0;
        this.layout = new InventoryLayout(data?.layout);
    }
}
export default CaptureTheWool;
//# sourceMappingURL=CaptureTheWool.js.map